消息队列 写一套基于消息队列 的节点程序管理。

总体流程大致为：
节点CS1到CS2到ZS
CS2发送短信(进入短信程序队列)，收到短信确认后，
成功进入zs节点
失败进入异常队列


1 每进入一个节点自动生成一个任务（Task） ，一个任务有多个行动（Action）
2 一个行动（Action）可以执行一个或多个程序（多个程序可以同时执行），所有程序执行结束后，更新任务状态 返回主流程节点，继续往下走。
3 如果出现了异常，去差错队列。


programCode :
OC:操作中心 输出下一节点
CS1:初审一队列
CS2:初审二
ZS:终审
SMG:短信队列

架构设计：

首先是操作中心队列
（决定 节点怎么走） 节点处理类（以handler结尾）（程序处理类以executor结尾）
方法：
1根据节点代码 转发到不同的队列
（先判断 是去程序队列，还是去节点队列）
每去一个节点 回生成一个任务 一个任务里面有一个至多个程序
所有程序执行完 才会去到下个节点

2自动生成任务 并且更新任务状态

3处理差错并且发送至差错中心




